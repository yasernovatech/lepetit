# راهنمای دیپلوی پروژه روی Vercel

## ✅ تغییرات انجام شده

### 1. فایلهای حذف شده:
- ✅ پوشه `.next` (فایلهای build قبلی)
- ✅ پوشه `out_original` (فایلهای اضافی)
- ✅ پوشه `temp_backup` (فایلهای موقت)
- ✅ فایل `out.zip` (فایل فشرده اضافی)

### 2. فایلهای اصلاح شده:
- ✅ `.gitignore` - کامل شده برای Next.js و Vercel
- ✅ `package.json` - اضافه شدن engines و اصلاح scripts
- ✅ `vercel.json` - ساخته شده برای تنظیمات دیپلوی

### 3. تست Build:
- ✅ Build با موفقیت انجام شد
- ✅ 36 صفحه static تولید شد
- ✅ پوشه `out` با موفقیت ساخته شد

---

## 📋 تنظیمات Vercel

### Build Command:
```bash
next build
```

### Output Directory:
```
out
```

### Install Command:
```bash
npm install
```

### Node Version:
```
18.x
```

---

## 🚀 مراحل Deploy

### مرحله 1: Push کردن تغییرات به GitHub

```bash
# اضافه کردن تمام تغییرات
git add .

# Commit کردن تغییرات
git commit -m "Fix: Prepare project for Vercel deployment"

# Push کردن به GitHub
git push origin main
```

اگر branch شما `master` است به جای `main` از `master` استفاده کنید:
```bash
git push origin master
```

---

### مرحله 2: اتصال به Vercel

1. به [vercel.com](https://vercel.com) بروید
2. با GitHub خود وارد شوید
3. روی "Add New Project" کلیک کنید
4. ریپازیتوری `yasernovatech/lepetit` را انتخاب کنید
5. روی "Import" کلیک کنید

---

### مرحله 3: تنظیمات پروژه در Vercel

Vercel به صورت خودکار تنظیمات را از `vercel.json` میخواند، اما برای اطمینان:

#### Framework Preset:
```
Next.js
```

#### Build Command:
```
next build
```

#### Output Directory:
```
out
```

#### Install Command:
```
npm install
```

#### Root Directory:
```
./
```

---

### مرحله 4: Environment Variables (اختیاری)

این پروژه نیازی به Environment Variables ندارد، اما اگر در آینده نیاز شد:

1. در تنظیمات پروژه Vercel به بخش "Environment Variables" بروید
2. متغیرهای مورد نیاز را اضافه کنید

---

### مرحله 5: Deploy

1. روی "Deploy" کلیک کنید
2. منتظر بمانید تا Build تمام شود (حدود 2-3 دقیقه)
3. پس از اتمام، لینک پروژه شما نمایش داده میشود

---

## 🔄 Deploy های بعدی

پس از اولین Deploy، هر بار که تغییری به GitHub push کنید، Vercel به صورت خودکار پروژه را دوباره Deploy میکند.

```bash
git add .
git commit -m "Your commit message"
git push origin main
```

---

## ⚠️ نکات مهم

1. **حذف node_modules**: این پوشه در `.gitignore` است و نباید به GitHub push شود
2. **حذف .next و out**: این پوشهها در `.gitignore` هستند و Vercel آنها را خودش میسازد
3. **Static Export**: این پروژه با `output: 'export'` تنظیم شده و فایلهای static تولید میکند
4. **Images**: تصاویر با `unoptimized: true` تنظیم شدهاند برای static export

---

## 🐛 عیب‌یابی

### اگر Build با خطا مواجه شد:

1. **خطای Dependencies**:
   ```bash
   npm install
   npm run build
   ```
   اگر در local کار کرد، در Vercel هم کار میکند.

2. **خطای Memory**:
   در تنظیمات Vercel، Node.js Memory را افزایش دهید.

3. **خطای Timeout**:
   Build Command را به این تغییر دهید:
   ```bash
   npm install && next build
   ```

---

## ✅ چک‌لیست نهایی

- [x] فایل `.gitignore` کامل شده
- [x] فایل `package.json` اصلاح شده (engines اضافه شده)
- [x] فایل `vercel.json` ساخته شده
- [x] پوشههای `.next` و `out` حذف شدهاند
- [x] Build در local با موفقیت انجام شد
- [ ] تغییرات به GitHub push شدهاند
- [ ] پروژه در Vercel import شده
- [ ] Deploy با موفقیت انجام شده

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:
1. لاگهای Build را در Vercel بررسی کنید
2. مطمئن شوید که تمام تغییرات به GitHub push شدهاند
3. Build را در local دوباره تست کنید

---

**موفق باشید! 🎉**
