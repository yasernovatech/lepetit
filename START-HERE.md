# 🚀 شروع سریع - START HERE

## برای آپلود سایت به هاست، فقط 3 مرحله!

---

## ⚡ مرحله 1: ساخت فایلها (2 دقیقه)

### روش 1: دابل کلیک
روی فایل `build-and-deploy.bat` دابل کلیک کنید

### روش 2: دستور
```bash
npm install
npm run build
```

### نتیجه:
فولدر `out` ایجاد میشود ✅

---

## ⚡ مرحله 2: تست (1 دقیقه)

روی فایل `test-build.bat` دابل کلیک کنید

### باید ببینید:
```
✓ index.html found
✓ _next folder found
✓ CSS folder found
✓ images folder found
```

---

## ⚡ مرحله 3: آپلود (5 دقیقه)

### با FileZilla:
1. به هاست متصل شوید
2. به پوشه `public_html` بروید
3. **تمام محتویات** فولدر `out` را بکشید داخل (نه خود فولدر)
4. صبر کنید تا آپلود تمام شود

### با cPanel:
1. File Manager → public_html
2. فایلهای قدیمی را پاک کنید
3. Upload → Select Files
4. محتویات فولدر `out` را انتخاب کنید
5. آپلود کنید

---

## ✅ تست سایت

1. سایت را باز کنید: `http://yourdomain.com`
2. F12 را بزنید
3. Console را چک کنید - نباید خطایی باشد

---

## ❌ اگر مشکل داشتید

### مشکل 1: صفحه بدون استایل
→ فایل `TROUBLESHOOTING.md` را باز کنید
→ بخش "صفحات بدون استایل" را بخوانید

### مشکل 2: تصاویر نمایش داده نمیشوند
→ فایل `TROUBLESHOOTING.md` را باز کنید
→ بخش "تصاویر نمایش داده نمیشوند" را بخوانید

### مشکل 3: صفحه 404
→ فایل `TROUBLESHOOTING.md` را باز کنید
→ بخش "صفحه 404 Not Found" را بخوانید

---

## 📚 فایلهای مهم

| فایل | توضیح |
|------|-------|
| `START-HERE.md` | همین فایل - شروع سریع |
| `README-FA.md` | راهنمای کامل فارسی |
| `HOSTING-GUIDE.md` | راهنمای آپلود به هاست |
| `TROUBLESHOOTING.md` | رفع مشکلات |
| `UPLOAD-CHECKLIST.md` | چک لیست آپلود |
| `build-and-deploy.bat` | ساخت فایلها |
| `test-build.bat` | تست فایلها |

---

## 🎯 نکات مهم

### ✅ انجام دهید:
- تمام محتویات `out` را آپلود کنید
- فایل `.htaccess` را آپلود کنید
- مجوزها را درست تنظیم کنید (644/755)

### ❌ انجام ندهید:
- خود فولدر `out` را آپلود نکنید
- فایلها را یکی یکی آپلود نکنید
- فولدر `_next` را فراموش نکنید

---

## 💡 نکته طلایی

**هر بار که کد را تغییر دادید:**
1. `build-and-deploy.bat` را اجرا کنید
2. فایلهای جدید را آپلود کنید
3. Cache مرورگر را پاک کنید (Ctrl+Shift+Delete)

---

## 🆘 کمک فوری

اگر گیر کردید:
1. فایل `TROUBLESHOOTING.md` را باز کنید
2. مشکل خود را پیدا کنید
3. راه حل را دنبال کنید

اگر باز هم حل نشد:
1. F12 → Console
2. Screenshot بگیرید
3. با پشتیبانی هاست تماس بگیرید

---

## 🎉 موفق باشید!

با انجام این 3 مرحله ساده، سایت شما آماده است!

**سوال دارید؟** فایل `README-FA.md` را بخوانید.
