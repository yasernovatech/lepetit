# راهنمای آپلود وبسایت به Namecheap

## مرحله 1: بیلد پروژه

1. ترمینال را در پوشه پروژه باز کنید
2. دستور زیر را اجرا کنید:

```bash
npm run build
```

این دستور پوشه `out` را با فایلهای استاتیک ایجاد میکند.

---

## مرحله 2: آماده‌سازی فایل‌ها

پس از بیلد، پوشه `out` شامل این فایل‌ها است:
- فایل‌های HTML
- فایل‌های CSS و JavaScript
- پوشه `images` با تصاویر
- فایل‌های استاتیک دیگر

---

## مرحله 3: ورود به cPanel Namecheap

1. به پنل Namecheap خود وارد شوید
2. به بخش **Hosting List** بروید
3. روی **Manage** کلیک کنید
4. روی **cPanel** کلیک کنید

---

## مرحله 4: آپلود فایل‌ها

### روش 1: استفاده از File Manager (توصیه میشود)

1. در cPanel، **File Manager** را باز کنید
2. به پوشه `public_html` بروید
3. تمام فایل‌های قدیمی را حذف کنید (اگر وجود دارد)
4. روی **Upload** کلیک کنید
5. تمام محتویات پوشه `out` را انتخاب و آپلود کنید

**نکته مهم:** فایل‌ها را مستقیماً در `public_html` قرار دهید، نه در زیرپوشه!

### روش 2: استفاده از FTP (برای فایل‌های زیاد)

1. از FileZilla یا WinSCP استفاده کنید
2. اطلاعات FTP را از cPanel دریافت کنید:
   - Host: ftp.yourdomain.com
   - Username: از cPanel
   - Password: از cPanel
   - Port: 21
3. به پوشه `public_html` متصل شوید
4. تمام محتویات پوشه `out` را آپلود کنید

---

## مرحله 5: تنظیمات .htaccess

یک فایل `.htaccess` در `public_html` ایجاد کنید با محتوای زیر:

```apache
# Enable HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Remove .html extension
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Custom error pages
ErrorDocument 404 /404.html

# Enable compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Browser caching
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

---

## مرحله 6: تست وبسایت

1. به آدرس دامنه خود بروید: `https://yourdomain.com`
2. تمام صفحات را تست کنید:
   - صفحه اصلی
   - صفحه محصولات
   - صفحه درباره ما
   - صفحه تماس
   - سبد خرید

---

## مرحله 7: تنظیمات SSL (اختیاری اما توصیه میشود)

1. در cPanel به **SSL/TLS Status** بروید
2. روی **Run AutoSSL** کلیک کنید
3. منتظر بمانید تا گواهی SSL نصب شود

---

## نکات مهم:

✅ **همیشه قبل از آپلود، بکآپ بگیرید**
✅ **فایل‌های .env را آپلود نکنید**
✅ **پوشه node_modules را آپلود نکنید**
✅ **فقط محتویات پوشه out را آپلود کنید**

---

## عیب‌یابی مشکلات رایج:

### مشکل 1: صفحه 404 نشان میدهد
- بررسی کنید فایل‌ها در `public_html` هستند نه در زیرپوشه
- فایل `.htaccess` را بررسی کنید

### مشکل 2: تصاویر نمایش داده نمیشوند
- مطمئن شوید پوشه `images` آپلود شده
- مسیرهای تصاویر را بررسی کنید

### مشکل 3: CSS/JS کار نمیکند
- Cache مرورگر را پاک کنید
- فایل‌های `_next` را بررسی کنید

---

## دستورات سریع:

```bash
# بیلد پروژه
npm run build

# بررسی پوشه out
dir out

# فشرده‌سازی برای آپلود سریع‌تر
# پوشه out را به zip تبدیل کنید و در cPanel آپلود و Extract کنید
```

---

## پشتیبانی:

اگر مشکلی داشتید:
1. لاگ‌های خطا را در cPanel > Error Log بررسی کنید
2. با پشتیبانی Namecheap تماس بگیرید
3. فایل `.htaccess` را موقتاً حذف کنید و دوباره تست کنید

---

**موفق باشید! 🚀**
