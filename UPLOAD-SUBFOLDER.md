# 📁 راهنمای آپلود در زیرشاخه /out

## 🎯 آدرس سایت شما:
```
https://novatechsoft.com/out/
```

## 📤 مراحل آپلود:

### روش 1: با FileZilla/FTP

1. به هاست متصل شوید
2. به پوشه `public_html` بروید
3. پوشه `out` ایجاد کنید (اگر وجود ندارد)
4. وارد پوشه `out` شوید
5. **تمام محتویات** فولدر `out` (از کامپیوتر) را داخل این پوشه آپلود کنید

### روش 2: با cPanel

1. File Manager → public_html
2. Create Folder → نام: `out`
3. وارد پوشه `out` شوید
4. Upload → تمام فایلهای داخل فولدر `out` را انتخاب کنید
5. آپلود کنید

## 📂 ساختار نهایی در هاست:

```
public_html/
├── (فایلهای سایت قبلی شما)
└── out/
    ├── index.html
    ├── _next/
    ├── images/
    ├── .htaccess
    └── ...
```

## ✅ تست سایت:

بعد از آپلود، این آدرسها را باز کنید:

1. صفحه اصلی:
   ```
   https://novatechsoft.com/out/
   ```

2. تست سلامت:
   ```
   https://novatechsoft.com/out/health-check.html
   ```

3. صفحه محصولات:
   ```
   https://novatechsoft.com/out/products/
   ```

## ⚠️ نکات مهم:

- ✅ سایت قبلی شما دست نخورده میماند
- ✅ فقط پوشه `/out` اضافه میشود
- ✅ تمام لینکها به صورت خودکار `/out` را در ابتدا دارند
- ✅ استایلها و تصاویر به درستی کار میکنند

## 🔄 اگر میخواهید سایت در root باشد:

اگر بعداً خواستید سایت در `novatechsoft.com` (بدون /out) باشد:

1. فایل `next.config.mjs` را باز کنید
2. این خطوط را تغییر دهید:
   ```javascript
   basePath: '',
   assetPrefix: '',
   ```
3. دوباره `npm run build` کنید
4. محتویات `out` را در `public_html` آپلود کنید

---

**آماده آپلود است! 🚀**
