# فروشگاه اولیا - Olea Store

## 🎯 مشکل شما و راه حل

### مشکل: صفحات بدون استایل در هاست
وقتی سایت Next.js را به صورت استاتیک export میکنید و روی هاست آپلود میکنید، ممکن است با این مشکلات مواجه شوید:
- صفحات بدون CSS و استایل
- تصاویر نمایش داده نمیشوند
- فونتها لود نمیشوند
- لینکها کار نمیکنند

### علت مشکل:
1. **مسیرهای نسبی اشتباه** - Next.js در حالت export باید مسیرها را درست تنظیم کند
2. **فایل .htaccess نبودن** - برای routing در Apache
3. **تنظیمات next.config.mjs** - باید برای static export بهینه شود
4. **فایلهای CSS در مسیر اشتباه** - باید در `_next/static/css/` باشند

## ✅ راه حلهای اعمال شده

### 1. تنظیمات next.config.mjs
```javascript
- basePath و assetPrefix اضافه شد
- webpack config برای حل مشکل CSS
- images.unoptimized برای هاستهای استاتیک
```

### 2. فایل .htaccess
- Routing برای Next.js
- Cache headers برای سرعت بیشتر
- Compression برای کاهش حجم
- Security headers

### 3. فایل .nojekyll
- جلوگیری از مشکلات Jekyll در برخی هاستها

### 4. اسکریپتهای بهبود یافته
- `build-and-deploy.bat` - ساخت فایلهای استاتیک
- `test-build.bat` - تست صحت build

## 🚀 مراحل استفاده

### مرحله 1: نصب وابستگیها
```bash
npm install
```

### مرحله 2: ساخت فایلهای استاتیک
روی فایل `build-and-deploy.bat` دابل کلیک کنید یا:
```bash
npm run build
```

### مرحله 3: تست Build
روی فایل `test-build.bat` دابل کلیک کنید تا مطمئن شوید همه چیز درست است.

### مرحله 4: آپلود به هاست

#### روش 1: FTP (FileZilla)
1. به هاست متصل شوید
2. به پوشه `public_html` بروید
3. **تمام محتویات** فولدر `out` را آپلود کنید (نه خود فولدر)
4. مطمئن شوید این موارد آپلود شدند:
   - ✅ `index.html`
   - ✅ فولدر `_next/`
   - ✅ فولدر `images/`
   - ✅ `.htaccess`
   - ✅ `.nojekyll`

#### روش 2: cPanel
1. File Manager را باز کنید
2. به `public_html` بروید
3. فایلهای قدیمی را پاک کنید
4. محتویات `out` را آپلود کنید

## 📁 ساختار فولدر out

بعد از build موفق، فولدر `out` این ساختار را دارد:

```
out/
├── index.html              # صفحه اصلی
├── about.html              # صفحه درباره ما
├── contact.html            # صفحه تماس
├── products.html           # صفحه محصولات
├── .htaccess              # تنظیمات Apache
├── .nojekyll              # تنظیمات Jekyll
├── _next/                 # فایلهای Next.js
│   ├── static/
│   │   ├── css/          # ⭐ فایلهای CSS اینجا هستند
│   │   ├── chunks/       # فایلهای JavaScript
│   │   └── media/        # فونتها
└── images/                # تصاویر سایت
```

## 🔧 رفع مشکلات رایج

### ❌ مشکل 1: صفحات بدون استایل
**علامت:** صفحه باز میشود اما بدون CSS و طراحی

**راه حل:**
1. F12 را بزنید و Console را چک کنید
2. اگر خطای 404 برای فایلهای CSS دیدید:
   - مطمئن شوید فولدر `_next` کامل آپلود شده
   - فایلهای CSS در `_next/static/css/` موجود باشند
3. مجوزهای فایلها را چک کنید:
   - فایلها: 644
   - فولدرها: 755

### ❌ مشکل 2: تصاویر نمایش داده نمیشوند
**راه حل:**
1. مطمئن شوید فولدر `images` در root آپلود شده
2. مسیرهای تصاویر در کد با `/images/` شروع شوند
3. مجوزهای فولدر images را 755 کنید

### ❌ مشکل 3: صفحه 404
**راه حل:**
1. فایل `.htaccess` را آپلود کنید
2. مطمئن شوید `index.html` در root است
3. در cPanel، Document Root را چک کنید

### ❌ مشکل 4: فونتها لود نمیشوند
**راه حل:**
1. فایلهای فونت در `_next/static/media/` باشند
2. در cPanel > MIME Types این موارد را اضافه کنید:
   - `.woff` → `font/woff`
   - `.woff2` → `font/woff2`

## 🎨 ویژگیهای سایت

- ✅ طراحی مینیمال و مدرن
- ✅ پشتیبانی از فارسی و آلمانی
- ✅ Responsive (موبایل، تبلت، دسکتاپ)
- ✅ بهینه شده برای SEO
- ✅ سرعت بالا با Static Export
- ✅ تصاویر بهینه شده
- ✅ فونتهای وب بهینه

## 📊 صفحات سایت

1. **صفحه اصلی** (`/`) - Hero با گالری محصولات
2. **محصولات** (`/products`) - لیست تمام محصولات
3. **محصولات لاکچری** (`/luxury-products`) - محصولات ویژه
4. **درباره ما** (`/about`) - معرفی فروشگاه
5. **تماس با ما** (`/contact`) - فرم تماس
6. **جزئیات محصول** (`/product/[slug]`) - صفحه تک محصول

## 🛠️ تکنولوژیها

- **Framework:** Next.js 15.5.6
- **UI Library:** React 19.2.0
- **Styling:** Tailwind CSS 3.4.17
- **Components:** Radix UI
- **Icons:** Lucide React
- **Fonts:** Vazirmatn (فارسی) + Playfair Display (انگلیسی)
- **Analytics:** Vercel Analytics

## 📝 نکات مهم

1. **هر بار تغییر در کد:**
   - دوباره `npm run build` کنید
   - فایلهای جدید را آپلود کنید

2. **بهینهسازی سرعت:**
   - از CDN استفاده کنید (مثل Cloudflare)
   - Gzip compression را فعال کنید
   - Browser caching را تنظیم کنید

3. **امنیت:**
   - SSL/HTTPS را فعال کنید
   - فایل `.htaccess` را آپلود کنید
   - مجوزهای فایلها را درست تنظیم کنید

## 🆘 پشتیبانی

اگر بعد از انجام تمام مراحل همچنان مشکل دارید:

1. **Console Browser را چک کنید:**
   - F12 → Console
   - خطاهای قرمز را بخوانید

2. **Network Tab را بررسی کنید:**
   - F12 → Network
   - ببینید کدام فایلها 404 هستند

3. **مسیرها را بررسی کنید:**
   - آیا فایلها در مسیر درست هستند؟
   - آیا نام فایلها درست است؟

4. **با هاست تماس بگیرید:**
   - بپرسید آیا mod_rewrite فعال است
   - بپرسید آیا .htaccess پشتیبانی میشود

## 📞 اطلاعات تماس

- **نام سایت:** فروشگاه اولیا - Olea Store
- **نوع:** فروشگاه آنلاین پوشاک و اسباببازی
- **زبانها:** فارسی (اصلی) + آلمانی

---

**موفق باشید! 🎉**

اگر همه مراحل را درست انجام دهید، سایت شما با تمام استایلها و طراحیها به درستی کار خواهد کرد.
